<style>
    .product-card {
        background-color: #f8f9fa;
        cursor: pointer;
        transition: transform 0.2s ease-in-out, background-color 0.3s ease-in-out;
        text-decoration: none;
        color: inherit;
        display: block;
        border-radius: 0.5rem;
    }

        .product-card:hover {
            transform: scale(1.02);
            background-color: #e9ecef;
        }

    .card-body {
        position: relative;
        padding-bottom: 3rem;
    }

    .btn-delete {
        position: absolute;
        top: 0.5rem;
        right: 0.5rem;
        z-index: 2;
        opacity: 0.9;
        transition: opacity 0.2s ease;
    }

    .btn-edit {
        position: absolute;
        bottom: 0.75rem;
        right: 0.75rem;
        z-index: 2;
        opacity: 0.9;
        transition: opacity 0.2s ease;
    }

    .product-card:hover .btn-delete,
    .product-card:hover .btn-edit {
        opacity: 1;
    }
</style>



<div class="row row-cols-1 row-cols-md-3 g-4">
    @foreach (var item in Model)
    {
        var availableUntil = item.StartDate.AddHours(item.DurationInHours);

        <div class="col">
            <div class="card h-100 shadow-sm border-0 product-card">
                <div class="card-body">

                    <form asp-action="Delete" asp-route-id="@item.Id" method="post"
                          onsubmit="return confirm('Are you sure you want to delete this product?');">
                        <button type="submit" class="btn btn-sm btn-danger btn-delete" title="Delete">
                            &times;
                        </button>
                    </form>

                    <h5 class="card-title text-center">@item.Name</h5>
                    <p class="card-text mb-1"><strong>Price:</strong> @item.Price.ToString("C")</p>
                    <p class="card-text mb-1"><strong>Category:</strong> @item.Category?.Name</p>
                    <p class="card-text mb-1"><strong>Start:</strong> @item.StartDate.ToString("g")</p>
                    <p class="card-text"><strong>Until:</strong> @availableUntil.ToString("g")</p>


                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary btn-edit">Edit</a>


                    <a asp-action="Details" asp-route-id="@item.Id" class="stretched-link"></a>
                </div>
            </div>
        </div>
    }
</div>